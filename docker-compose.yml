###############################################################################
# Benki Multi-Agent Crypto System — Docker Compose
# 4 services: PostgreSQL + 3 hermes-agent gateway instances
###############################################################################

services:
  # ─── PostgreSQL (trade logs, P&L, risk audit) ───────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: benki-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: benki
      POSTGRES_USER: benki
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - benki-net
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U benki"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Benki Main (Orchestrator) ──────────────────────────────────────
  benki-main:
    image: nousresearch/hermes-agent:latest
    container_name: benki-main
    restart: unless-stopped
    user: "root"
    entrypoint:
      - /bin/bash
      - -c
      - |
        source /opt/hermes/.venv/bin/activate
        mkdir -p /opt/data/{cron,sessions,logs,hooks,memories,skills,skins,plans,workspace,home}
        cp /opt/config.yaml /opt/data/config.yaml
        chown -R hermes:hermes /opt/data
        exec gosu hermes hermes gateway
    env_file:
      - ./configs/main/.env
    environment:
      HERMES_HOME: /opt/data
      # Enable the Hermes HTTP API so benki-ui can poll /health
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_PORT: "8642"
      API_SERVER_KEY: ${API_SERVER_KEY}
    volumes:
      - main-data:/opt/data
      # Add new mount for cron loops
      - ./cron/main:/opt/data/cron:ro
      # Plugin: risk_manager
      - ./plugins/risk-manager:/opt/data/plugins/risk-manager
      # Plugin: sentiment_parser
      - ./plugins/sentiment-parser:/opt/data/plugins/sentiment-parser
      # Plugin: db_client
      - ./plugins/db-client:/opt/data/plugins/db-client
      # Plugin: market_fetch (live prices + Polymarket, no API key required)
      - ./plugins/market-fetch:/opt/data/plugins/market-fetch
      # Config overlay
      - ./configs/main/config.yaml:/opt/config.yaml:ro
      # Skills
      - ./skills/main:/opt/data/skills/custom
    networks:
      - benki-net
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  # ─── Benki Trader (DeFi Execution) ──────────────────────────────────
  benki-trader:
    image: nousresearch/hermes-agent:latest
    container_name: benki-trader
    restart: unless-stopped
    user: "root"
    entrypoint:
      - /bin/bash
      - -c
      - |
        source /opt/hermes/.venv/bin/activate
        mkdir -p /opt/data/{cron,sessions,logs,hooks,memories,skills,skins,plans,workspace,home}
        cp /opt/config.yaml /opt/data/config.yaml
        chown -R hermes:hermes /opt/data
        exec gosu hermes hermes gateway
    env_file:
      - ./configs/trader/.env
    environment:
      HERMES_HOME: /opt/data
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_PORT: "8642"
      API_SERVER_KEY: ${API_SERVER_KEY}
    volumes:
      - trader-data:/opt/data
      # Add new mount for cron loops
      - ./cron/trader:/opt/data/cron:ro
      # Plugins
      - ./plugins/risk-manager:/opt/data/plugins/risk-manager
      - ./plugins/evm-client:/opt/data/plugins/evm-client
      - ./plugins/solana-client:/opt/data/plugins/solana-client
      - ./plugins/db-client:/opt/data/plugins/db-client
      # Config overlay
      - ./configs/trader/config.yaml:/opt/config.yaml:ro
      # Skills
      - ./skills/trader:/opt/data/skills/custom
    networks:
      - benki-net
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2560M
        reservations:
          memory: 512M

  # ─── Benki UI (Web Dashboard) ───────────────────────────────────
  benki-ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    container_name: benki-ui
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://benki:${POSTGRES_PASSWORD}@postgres:5432/benki
      # Must match the API_SERVER_KEY set on the agent services above
      API_SERVER_KEY: ${API_SERVER_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      GATEWAY_PORT: "8642"
      HEALTH_PATH: /health
      MAIN_HOST: benki-main
      TRADER_HOST: benki-trader
      PREDICTOR_HOST: benki-predictor
    networks:
      - benki-net
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  # ─── Benki Predictor (Prediction Markets) ───────────────────────────
  benki-predictor:
    image: nousresearch/hermes-agent:latest
    container_name: benki-predictor
    restart: unless-stopped
    user: "root"
    entrypoint:
      - /bin/bash
      - -c
      - |
        source /opt/hermes/.venv/bin/activate
        mkdir -p /opt/data/{cron,sessions,logs,hooks,memories,skills,skins,plans,workspace,home}
        cp /opt/config.yaml /opt/data/config.yaml
        chown -R hermes:hermes /opt/data
        exec gosu hermes hermes gateway
    env_file:
      - ./configs/predictor/.env
    environment:
      HERMES_HOME: /opt/data
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_PORT: "8642"
      API_SERVER_KEY: ${API_SERVER_KEY}
    volumes:
      - predictor-data:/opt/data
      # Add new mount for cron loops
      - ./cron/predictor:/opt/data/cron:ro
      # Plugins
      - ./plugins/risk-manager:/opt/data/plugins/risk-manager
      - ./plugins/evm-client:/opt/data/plugins/evm-client
      - ./plugins/solana-client:/opt/data/plugins/solana-client
      - ./plugins/polymarket-client:/opt/data/plugins/polymarket-client
      - ./plugins/drift-bet-client:/opt/data/plugins/drift-bet-client
      - ./plugins/db-client:/opt/data/plugins/db-client
      # Config overlay
      - ./configs/predictor/config.yaml:/opt/config.yaml:ro
      # Skills
      - ./skills/predictor:/opt/data/skills/custom
    networks:
      - benki-net
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2560M
        reservations:
          memory: 512M

volumes:
  pgdata:
  main-data:
  trader-data:
  predictor-data:

networks:
  benki-net:
    driver: bridge
